PKG_NAME={{ pkg.name }}
PKG_URL={{ pkg.url }}
PKG_VERSION={{ pkg.hash }}
PKG_LICENSE={{ pkg.pkg_license }}

include $(RIOTBASE)/pkg/pkg.mk

all:
	$(QQ)"$(MAKE)" -C $(PKG_SOURCE_DIR) -f $(CURDIR)/$(PKG_NAME).mk
